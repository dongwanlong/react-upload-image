define(['angular',
      'src/apps/cloudvm/controllers/VmSnapshotCrtl',
      'angularMocks'],
    function(angular, Utility) {
/*        describe('VmSnapshotCrtl.test', function () {
            beforeEach(module('app.controller'));

            var $controller;

            beforeEach(inject(function (_$controller_) {
                // The injector unwraps the underscores (_) from around the parameter names when matching
                $controller = _$controller_;
            }));

            it('$scope.confirmMessage should equal to message', function () {
                $controller('VmSnapshotCrtl', {
                    '$scope':{},'$interval':{},'$modal':{},'$q':{}, 'Config':{urls:{snapshot_vm_list:'snapshot/vm/list'}}, 'HttpService':{doGet:function(){}},'WidgetService':{},'CurrentContext':{}
                });
                expect($scope.tabShow).toEqual('VmSnapshot');
            });
        });*/
    }
);